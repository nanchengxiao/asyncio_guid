# Practice — Job Processing Service

先完成 DESIGN.md，再编码。必须明确输入 backlog、worker、API concurrency、QPS、timeout、retry、idempotency、writer capacity 与 shutdown。

验收：`uv run pytest lessons/11_production_asyncio/tests -v --learner`
