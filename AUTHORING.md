# Course Authoring Contract

本文档只面向课程维护者。学习者不需要先读它。

## 唯一默认前置

课程只允许默认学习者已经掌握**普通 Python 基础**：基本语法、函数、常见容器、模块导入和基础异常处理。

除此之外，不默认学习者知道高级 Python、asyncio、并发、网络、线程或生产工程术语。

## 首次出现规则

**任何新术语第一次进入主线时，必须先用一句白话说明“它在代码或业务里做什么”，再使用术语本身。**

“第一次进入主线”包括：

- Lesson 标题和开场文字；
- `进入本课前`；
- `本节目标`；
- `为什么需要学习它`；
- 理论正文、图示和代码注释；
- `关键问题`、`场景命题`、`验收`；
- `case.py` 与 `practice.py` 中面向学习者的注释。

不要因为某个词“工程师都知道”就跳过定义。HTTP、URL、JSON、API、SDK、连接池、线程池、QPS、幂等性等也属于需要首次解释的词。

不要在前一课用“下一课会学习 X”提前泄露 X 的术语名。需要预告时，用白话描述下一课要解决的问题即可。

## 每课文档顺序

每课目录固定包含三个文件：`<课程名>.md`（与目录同名的讲义）、`case.py`、`practice.py`。

讲义统一按下面顺序组织：

1. `## 进入本课前`
2. `## 本课新增术语`
3. `## 本节目标`
4. `## 为什么需要学习它`
5. `## 核心理论`
6. `## 脑内执行模型`
7. `## 常见误解`
8. `## 本节规则总结`
9. `## 关键问题`
10. `## 场景命题`

在 `本课新增术语` 与 `本节目标` 之间，允许插入一节完整的示例（例如“一个例子串起全部术语”），把本课术语落到同一段可运行代码上；该小节为可选。

`进入本课前` 只能引用普通 Python 或前课已经正式解释过的概念。

`本课新增术语` 必须出现在目标和动机之前，避免“先要求会说一个词，再在后面解释它”。

## 定义写法

优先写成：

```text
- **术语（中文名）**：一句能落到代码行为或业务结果上的白话定义。
```

例如：

```text
- **Task（任务）**：被调度器正式登记、拥有自己执行进度的一份异步工作。
```

定义不要用两个尚未解释的新词互相解释。必须依赖另一个新词时，先定义被依赖的词。

## 后续课程如何复用术语

一个术语在前课已经正式解释后，后续课程可以直接使用，不重复铺陈同一段定义。

后续课程如果需要增加新语义，应写“在前面定义的基础上，本课再增加……”，只补充新增约束。

例如：前课已经解释“重试就是失败后再尝试”，生产课只需要继续讨论“哪些失败可重试、最多几次、重复执行是否安全”，不再重新做基础定义。

## 导航文档职责

根 `README.md` 只保留：

- 课程定位；
- 前置要求；
- 学习路线；
- 运行方式；
- 仓库导航。

根 README 不记录教学规范、某次重构过程、优化说明、审稿规则或课程维护细节。

`COURSE_MAP.md` 是导航图，不承担术语教学。它只用白话描述“这一课解决什么问题”，避免在 Lesson 正式介绍前提前堆出术语表。

## 代码与练习

- 每课目录只包含三个文件：`<课程名>.md`、`case.py`、`practice.py`。
- `case.py` 是本课核心示例：典型真实且简洁，复制到任意位置都能直接运行；面向学习者的注释和 docstring 使用中文。
- `practice.py` 保持空文件，由学习者阅读讲义后对照 `case.py` 亲手实现。
- 示例与练习只能使用本课正文或前课已经解释过的 API 与术语。
- API 名、标识符和必要的英文术语保留原名。

## 审稿检查

提交课程文档前至少检查：

1. 从根 README 开始按实际学习顺序阅读，是否出现未解释的新词。
2. 每课 `本课新增术语` 是否位于 `本节目标` 之前。
3. 新定义是否真的用白话说明行为，而不是用另一个陌生词替换。
4. practice 是否偷偷引入了正文没讲过的词。
5. 后续课程是否在重复解释前课已经建立的定义。
6. 根 README 是否仍然只承担项目入口职责。
