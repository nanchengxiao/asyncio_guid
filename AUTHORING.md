# Course Authoring Contract

本文档只面向课程维护者。学习者不需要先读它。

## 唯一默认前置

课程只允许默认学习者已经掌握**普通 Python 基础**：基本语法、函数、常见容器、模块导入和基础异常处理。

除此之外，不默认学习者知道高级 Python、asyncio、并发、网络、线程或生产工程术语。

## 首次出现规则

**任何新术语第一次进入主线时，必须先用一句白话说明“它在代码或业务里做什么”，再使用术语本身。**

“第一次进入主线”包括：

- Lesson 标题和开场文字；
- `进入本课前`；
- `本节目标`；
- `为什么需要学习它`；
- 理论正文、图示和代码注释；
- `关键问题`、`场景命题`、`验收`；
- `case.py` 与 `practice.py` 中面向学习者的注释。

不要因为某个词“工程师都知道”就跳过定义。HTTP、URL、JSON、API、SDK、连接池、线程池、QPS、幂等性等也属于需要首次解释的词。

不要在前一课用“下一课会学习 X”提前泄露 X 的术语名。需要预告时，用白话描述下一课要解决的问题即可。

## 每课文档顺序

每课目录固定包含三个文件：`<课程名>.md`（与目录同名的讲义）、`case.py`、`practice.py`。

讲义统一按下面顺序组织：

1. `## 进入本课前`
2. `## 本课新增术语`
3. `## 一个例子串起全部术语`
4. `## 本节目标`
5. `## 为什么需要学习它`
6. `## 核心理论`
7. `## 脑内执行模型`
8. `## 常见误解`
9. `## 本节规则总结`
10. `## 关键问题`
11. `## 场景命题`

“一个例子串起全部术语”不是单纯贴代码。它固定包含四层信息：

1. `case.py` 的完整代码，而且必须与文件内容保持一致；
2. 一次真实或有明确占位符的代表性输出，并标出哪些顺序或耗时不构成保证；
3. “把本课知识点对到代码上”的映射表，逐项说明术语在变量、函数、边界或输出中的落点；
4. 按时间线解释从创建、等待、恢复、失败到 cleanup 的执行过程。

这四层分别回答“代码是什么”“能观察到什么”“每个词落在哪里”“运行时到底怎样推进”。不要把它们拆散到需要学习者来回搜索的位置。

`进入本课前` 只能引用普通 Python 或前课已经正式解释过的概念。

`本课新增术语` 必须出现在目标和动机之前，避免“先要求会说一个词，再在后面解释它”。

## 学习负担控制

课程面向有中等普通 Python 基础、但可以没有 asyncio 基础的学习者。完整不等于要求一次记完；一课的信息量较大时，必须给出明确的“第一遍只抓什么”，让学习者先形成主执行模型，再回头补术语和边界。

- 第一遍通关重点必须落在一个可观察的问题上，例如“Queue 满时谁会停住”，而不是“记住本课全部 API”。
- 术语多时先分组，再允许学习者把实验支架或次要细节暂时当作黑盒；正文后面仍要完整解释，不能用“黑盒”掩盖知识缺口。
- 综合练习要拆成逐步可运行的小关卡。每关只增加一个主要机制，避免学生写完大量代码后才第一次运行。
- 本地 server、failure simulator 等支架如果不是练习目标，应明确允许从 `case.py` 复用，让学生把注意力留给本课主线。
- 每课结尾提供下一课链接，减少学习者退出当前文件后重新寻找路线的摩擦。
- 判断是否可以继续下一课，优先看学习者能否复述“脑内执行模型”并解释核心输出，不以背完术语表为标准。

## 定义写法

优先写成：

```text
- **术语（中文名）**：一句能落到代码行为或业务结果上的白话定义。
```

例如：

```text
- **Task（任务）**：被调度器正式登记、拥有自己执行进度的一份异步工作。
```

定义不要用两个尚未解释的新词互相解释。必须依赖另一个新词时，先定义被依赖的词。

术语较多时按执行层次分组，例如“业务结果 / asyncio 机制 / resource 支架”，并明确告诉学习者此时只需建立位置感，不必在看到完整例子前一次背完。分组不能破坏依赖顺序：一个定义里出现的本课新词，必须已经排在它前面。

## 后续课程如何复用术语

一个术语在前课已经正式解释后，后续课程可以直接使用，不重复铺陈同一段定义。

后续课程如果需要增加新语义，应写“在前面定义的基础上，本课再增加……”，只补充新增约束。

例如：前课已经解释“重试就是失败后再尝试”，生产课只需要继续讨论“哪些失败可重试、最多几次、重复执行是否安全”，不再重新做基础定义。

## 导航文档职责

根 `README.md` 只保留：

- 课程定位；
- 前置要求；
- 学习路线；
- 运行方式；
- 仓库导航。

根 README 不记录教学规范、某次重构过程、优化说明、审稿规则或课程维护细节。

`COURSE_MAP.md` 是导航图，不承担术语教学。它只用白话描述“这一课解决什么问题”，避免在 Lesson 正式介绍前提前堆出术语表。

## 代码与练习

- 每课目录只包含三个文件：`<课程名>.md`、`case.py`、`practice.py`。
- `case.py` 是本课核心示例：典型、真实、简洁，复制到任意位置都能直接运行；面向学习者的注释和 docstring 使用中文。
- 取舍顺序固定为：**先保证写法典型，再追求代码简洁**。不能为了少几行而牺牲清楚的 ownership、失败分类、resource cleanup 或业务语义。
- 核心示例必须可读、心智负担低，并有一条占主导的观察主线。不要为了显得“全面”把无关机制塞进同一个文件；深入边界优先放进讲义正文。
- 变量与函数名优先直接表达业务角色；除极短且含义显然的局部位置外，不用单字母或缩写迫使学习者先解码名称。
- Task 粒度跟随真正需要独立 scheduling 和 lifecycle 管理的并发工作；不要因为 DAG 有几个 node、函数有几个调用，就机械创建同样数量的 Task。
- 测试场景要显式表达行为，避免用取模、隐藏约定或巧妙缩写让学习者先解谜。必须存在的 server、failure simulator 等支架要与本课主线清楚分层。
- “真实”不等于依赖不可控外部服务：可以使用本地 server 或短小 simulator，但必须说明模拟了什么、真实替换后还要补哪些 resource lifecycle。
- `practice.py` 保持空文件，由学习者阅读讲义后对照 `case.py` 亲手实现。
- 示例与练习只能使用本课正文或前课已经解释过的 API 与术语。
- API 名、标识符和必要的英文术语保留原名。
- 可变的 Queue、Semaphore、Lock、metrics 等运行状态通常由当前 operation 或 service lifecycle 创建，再显式传给 child Task；不要无意放成跨运行残留的模块全局状态。
- Resource ownership 必须能从代码直接定位；正常返回、普通失败、timeout 与 cancellation 路径都要检查 cleanup。
- Concurrency、backpressure、peak、retry 等行为要尽量通过开始/结束日志、计数或最终结果直接观察，不能只靠注释宣称正确。
- 示例输出应尽量稳定；并发下确实不保证的相邻顺序要明确说明，业务结果则应可重复验收。
- 为了教学而省略的生产约束必须明确写出边界，不能让最小示例暗示真实系统无需处理它。

## 审稿检查

提交课程文档前至少检查：

1. 从根 README 开始按实际学习顺序阅读，是否出现未解释的新词。
2. 每课 `本课新增术语` 是否位于 `本节目标` 之前。
3. 新定义是否真的用白话说明行为，而不是用另一个陌生词替换。
4. practice 是否偷偷引入了正文没讲过的词。
5. 后续课程是否在重复解释前课已经建立的定义。
6. 根 README 是否仍然只承担项目入口职责。
7. 讲义中的完整 Python 示例是否与对应 `case.py` 逐字一致。
8. 代表性输出、知识点映射和时间线是否都能由当前代码直接推出。
9. 是否实际运行过每个 `case.py`，而不只做语法检查。
10. 每个 `practice.py` 是否仍是 0 字节空文件，每课目录是否仍只有约定的三个文件。
11. 异常、timeout、cancellation 或提前退出时，resource 与观测计数是否仍能正确收尾。
12. 信息量较大的课是否给出第一遍通关重点，综合练习是否能分阶段运行。
13. 实验支架与本课主线是否分层，学生是否被要求默写并非学习目标的支架代码。
14. 学习者完成一课后，是否能直接找到下一课。
