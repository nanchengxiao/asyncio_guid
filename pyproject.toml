[project]
name = "asyncio-guide"
version = "0.3.0"
description = "从 asyncio 初学者到生产级异步工程能力的实践课程"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.9",
]

[tool.uv]
package = false
